# Moodiary 改进指南

> 状态标记：✅ 已修复 | 🔴 待修复（Bug/安全） | 🟠 待优化 | 🟡 待改进 | 🟢 待完善

---

## 零、平台清理 ✅

- **操作**: 删除 `macos/`、`web/`、`windows/` 平台目录及相关代码
- **变更**:
  - 删除平台目录 `macos/`、`web/`、`windows/`
  - 删除 `lib/main_web.dart`、`lib/components/window_buttons/window_buttons.dart`
  - 移除 `bitsdojo_window`、`msix` 依赖及 `pubspec.yaml` 中 Windows/macOS/Web 相关配置
  - 清理 `main_native.dart`、`home_view.dart`、`app_pages.dart` 中的桌面专用代码
- **状态**: 项目现仅保留 `android/`、`ios/` 两个平台目录，APK 构建验证通过

---

## 一、架构 & 设计模式

### 1. IsarUtil 全静态方法，职责过重

- **文件**: `lib/persistence/isar.dart`
- **问题**: `IsarUtil` 是一个 ~780 行的 God Class，包含日记 CRUD、分类管理、字体管理、同步记录、数据迁移、媒体嵌入等所有数据库操作，全部用 `static` 方法实现
- **影响**:
  - 无法被 mock/注入，严重阻碍单元测试
  - 无法替换底层实现（如未来从 Isar 迁移到其他数据库）
  - 业务逻辑（如 `_normalizeDiaryDomainTag`、WebDAV 自动同步触发）混入持久化层
- **建议**: 拆分为 `DiaryRepository`、`CategoryRepository`、`FontRepository` 等，通过接口抽象 + DI 注入

### 2. getDiaryByCategory 内存全量加载后过滤 ✅

- **文件**: `lib/persistence/isar.dart`
- ~~**问题**: 虽然有 `offset`/`limit` 参数，但先 `findAllAsync()` 全量加载到内存，再做 Dart 层过滤和分页~~
- **修复**: 指定分类时利用 `categoryId` 索引在 DB 层直接过滤（`where().categoryIdEqualTo()`），避免全量扫描。domain 过滤仍在内存进行（待 #3 将 domain 入库后可进一步优化）

### 3. Domain 信息用 @ignore 而非持久化 ✅

- **文件**: `lib/common/models/isar/diary.dart`、`lib/common/models/isar/category.dart`、`lib/common/models/isar/diary.g.dart`、`lib/common/models/isar/category.g.dart`、`lib/persistence/isar.dart`、`lib/merge/merge.dart`
- ~~**问题**: `domain` 字段标记 `@ignore` 不入库，每次查询后靠 tag 推断~~
- **修复**:
  - 移除 `@ignore`，给 `Diary.domain` (property 23) 和 `Category.domain` (property 5) 加上 `@Index()`
  - 手动更新 `.g.dart`：将 `domain` 加入 schema properties/indexes、更新 serialize/deserialize、新增 `domainEqualTo()` filter 扩展方法
  - 新增 `migrateDomainField()` 回填历史数据（根据 tags/id前缀推断）
  - 在 `MergeUtil.merge` 中通过 `domainFieldMigrated` 标志确保一次性执行
  - `getAllCategory`/`getAllCategoryAsync` 改为 `.where().domainEqualTo()` DB 层过滤

---

## 二、数据同步

### 4. WebDAV 同步缺乏原子性和冲突解决 🟠

- **文件**: `lib/utils/webdav_util.dart` 第 197-288 行
- **问题**:
  - sync.json 是单点故障：所有同步元数据存在一个 JSON 文件中，并发写入会互相覆盖
  - 无冲突合并：只比较 `lastModified` 字符串，同时在两台设备修改同一篇日记时，后同步的一方直接覆盖前者
  - 无事务保障：上传日记数据 → 更新 sync.json 是两步操作，中间失败会导致状态不一致
  - `unawaited()` 调用自动同步时，错误只打 debug 日志，用户无法感知同步失败
- **建议**: 引入版本向量或时间戳 + 设备 ID 的冲突检测机制；提供冲突 UI 让用户选择

### 5. WebDAV 密码明文存储 ✅

- **文件**: `lib/utils/webdav_util.dart`
- ~~**问题**: WebDAV 的 URL、用户名、密码直接存储在 `SharedPreferences`（明文），而非使用已有的 `SecureStorageUtil`~~
- **修复**: 凭据迁移至 `FlutterSecureStorage`，使用 `webDavUrl`/`webDavUser`/`webDavPassword` 三个独立 key。`SharedPreferences` 仅保留 `hasWebDavOption` 布尔标志。`initWebDav()` 启动时自动将旧版本明文凭据迁移到 SecureStorage

### 6. 新旧同步服务并存

- **文件**: `lib/services/sync/sync.dart` 和 `lib/utils/webdav_util.dart`
- **问题**: `services/sync/` 定义了抽象 `SyncService` 和 `WebdavSyncService`（带 TODO 空实现），但实际使用的是 `webdav_util.dart` 中独立的 `WebDavUtil` 单例。新的抽象层未被使用，构成死代码
- **建议**: 统一到 `SyncService` 抽象层，删除旧的 `WebDavUtil` 直接调用

---

## 三、安全

### 7. 时间窗口加密 salt 可预测

- **文件**: `lib/utils/aes_util.dart` 第 86-94 行
- **问题**: `_dailySalt()` 使用当天日期的 MD5，完全可预测；`_currentTimeSlot` 也是可推算的，安全性依赖于 MD5（已不推荐用于安全场景）
- **建议**: 使用安全随机数生成 salt，或使用 HMAC-SHA256 替代 MD5

### 8. 应用锁密码存储方式 ✅

- **文件**: `lib/persistence/pref.dart` 第 65 行
- ~~**问题**: 密码存储在 `SharedPreferences` 的 allowList 中（明文或简单编码），而非 `FlutterSecureStorage`~~
- **修复**: 密码改存 `FlutterSecureStorage`（key: `lockPassword`）。`LockLogic` 和 `RemovePasswordLogic` 在 `onReady` 中异步加载，并自动将旧版本 SharedPreferences 明文密码迁移到 SecureStorage

---

## 四、错误处理 & 健壮性

### 9. 硬编码子串偏移 ✅

- **文件**: `lib/utils/file_util.dart`（新层）、`lib/utils/webdav_util.dart`、`lib/services/sync/impl/webdav_impl.dart`、`lib/components/local_send/`
- ~~**问题**: 项目中多处硬编码 `substring(6, 42)` 提取 UUID，共 6 处，没有边界检查~~
- **修复**: 在 `FileUtil` 中新增 `videoNameToThumbnailName(String videoName)` 统一方法（含边界检查），全项目 6 处调用均替换为此方法

### 10. cleanFile 中逻辑错误 ✅

- **文件**: `lib/utils/file_util.dart`
- ~~**问题**: `Future.wait` 删除三类文件被放在了 `for (final path in audiosToDelete)` 循环内，意味着每删除一个 audio controller，就会重复执行全部文件删除操作~~
- **修复**: 已将 `Future.wait` 移至循环外，controller 清理与文件删除正确分离

### 11. exportIsar 未 close Isar 异常 ✅

- **文件**: `lib/persistence/isar.dart`
- ~~**问题**: `Isar.open` 和 `copyToFile` 如果抛异常，`isar.close()` 不会被调用（没有 try-finally），可能导致文件锁泄露~~
- **修复**: 已添加 `try-finally`，确保 `isar.close()` 在异常时也被调用

### 12. zipFileUseRust 中 Future.wait 存在顺序依赖 ✅

- **文件**: `lib/utils/file_util.dart`
- ~~**问题**: `exportIsar` 和 `zip.addFile`（添加导出的 isar 文件）被放在同一个 `Future.wait` 中并行执行。`addFile` 可能在 `exportIsar` 完成之前就执行，导致添加不存在或不完整的文件~~
- **修复**: 已拆分为串行执行：`await Future.wait([addDir×4])` → `await exportIsar()` → `await addFile()`

---

## 五、可测试性

### 13. 测试覆盖极低

- **目录**: `test/`
- **问题**: 仅有 3 个测试文件（`lru_test.dart`、`task_scheduler_test.dart`、`widget_test.dart`），对于 300+ 文件的项目来说，测试覆盖率接近于零。核心逻辑（同步、加密、数据迁移）完全没有测试
- **建议**: 优先为数据迁移、同步逻辑、加密解密添加单元测试

### 14. 全静态 + 单例模式阻碍 DI

- **涉及文件**: `IsarUtil`、`WebDavUtil`、`HttpUtil`、`ThemeUtil`、`PrefUtil`
- **问题**: 几乎所有核心依赖都无法注入替换，无法在测试中 mock
- **建议**: 引入服务定位器（已依赖 `get_it`）或 GetX 的 `Bind` 统一管理依赖生命周期

---

## 六、代码质量

### 15. PrefUtil 使用字符串 key，无类型安全

- **文件**: `lib/persistence/pref.dart`
- **问题**: 全项目通过裸字符串 key（如 `'lock'`、`'fontScale'`）访问配置，没有枚举约束。拼写错误只会在运行时暴露。`setDefaultValues()` 方法 ~90 行的重复模式可通过声明式配置表简化
- **建议**: 定义类型安全的 `PrefKey<T>` 枚举或 sealed class

### 16. 硬编码中文字符串

- **文件**: `lib/pages/edit/edit_logic.dart` 第 403 行、`lib/utils/file_util.dart` 第 117 行等
- **问题**: 项目已有 l10n 国际化体系，但仍有多处硬编码中文字符串，破坏了国际化一致性
- **建议**: 全局搜索中文字符串，统一迁移到 l10n

### 17. _listenCount 计算不准确 ✅

- **文件**: `lib/pages/edit/edit_logic.dart`
- ~~**问题**: 对于富文本，用 `selection.baseOffset`（光标位置）代替字数统计~~
- **修复**: 已改为 `_toPlainText().length`，对富文本使用实际纯文本字数

---

## 优先级总览

| 状态 | 严重级别 | 编号 | 问题 | 影响范围 |
|------|---------|------|------|---------|
| ✅ | — | #0 | 删除 macOS/Web/Windows 平台目录及相关代码 | 项目精简 |
| ✅ | 🔴 Bug | #10 | `cleanFile` 循环内重复删除 | 数据完整性 |
| ✅ | 🔴 Bug | #11 | `exportIsar` 缺少 try-finally，文件锁泄露 | 稳定性 |
| ✅ | 🔴 Bug | #12 | `zipFileUseRust` 并行依赖竞态 | 备份损坏 |
| ✅ | 🔴 安全 | #5 | WebDAV 密码明文存 SharedPreferences | 凭据泄露 |
| ✅ | 🟠 性能 | #2 | `getDiaryByCategory` 全量加载 | 大数据量卡顿 |
| ✅ | 🟡 质量 | #9 | 硬编码子串偏移 x6 处 | 脆弱性 |
| ✅ | 🟡 安全 | #8 | 应用锁密码存 SharedPreferences 明文 | 凭据泄露 |
| ✅ | 🟡 质量 | #17 | 富文本字数统计用光标位置代替实际字数 | 编辑页显示错误 |
| ✅ | 🟠 设计 | #3 | domain 不入库，全内存过滤 | 性能 & 一致性 |
| 🔴 | 🟠 设计 | #4 | sync.json 单文件无冲突解决 | 多设备同步丢数据 |
| 🔴 | 🟡 架构 | #1, #14 | 全静态/单例无 DI | 不可测试 |
| 🔴 |  维护 | #15 | PrefUtil 字符串 key 无类型安全 | 开发效率 |
| � |  质量 | #13 | 3 个测试文件 / 300+ 源文件 | 回归风险 |
| 🔴 | 🟢 i18n | #16 | 残余硬编码中文 | 国际化不完整 |
